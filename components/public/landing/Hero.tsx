/**
 * Hero component for the public landing page.
 * Renders the primary landing page hero headline.
 * This component does not handle complex state or routing.
 */

import React from "react";

export default function Hero() {
  return (
    <h1>Hero</h1>
  );
}
